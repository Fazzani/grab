language: generic
sudo: required
install: skip
cache: false
matrix:
  fast_finish: true
services:
  - docker
git:
  depth: false
branches:
  only:
  - master
if: (commit_message =~ ^daily OR type = "cron") AND (branch = master)
before_script:
  - sudo apt-get update -qq
  - sudo apt-get install libxml2-utils xmlstarlet jq -y
  - npm install -g xml2json-cli
script:
  - ./scripts/check_channels.sh *.xmltv

notifications:
  email:
    recipients:
      - tunisienheni@gmail.com
    on_success: change
    on_failure: change
